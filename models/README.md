### Map containing all saved models with naming convention of checkpoint{fed_type}{environment}{state_size}.pth.

### In this folder, the newest models are saved.

### In sub-folders earlier model versions are archived